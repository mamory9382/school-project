// This is an example function that takes a parameter "name" and returns a greeting message with the name included.
function greet(name) {
  return `Hello, ${name}!`;
}

// You can call this function by passing in a name as an argument. For example:
console.log(greet("Alice")); // Outputs "Hello, Alice!"
